@model List<Warehouse>

<div class="container">
    <div class="row pt-3 pb-3">
        <div class="col-6">
            <h2>Warehouse</h2>
        </div>
        <div class="col-6 text-end">
            <a class="btn btn-primary" asp-controller="Warehouse" asp-action="Create">
                Create New Warehouse
            </a>
        </div>
    </div>


    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>Name</th>
                <th>Address</th>
                <th>Phone</th>
                <th>Email</th>
            </tr>
        </thead>
        <tbody>
            @foreach(var obj in Model)
            {
                <tr>
                    <td>@obj.Name</td>
                    <td>@obj.Address</td>
                    <td>@obj.Phone</td>
                    <td>@obj.Email</td>
                    <td>
                        <div class="w-75 btn-group role-group">
                            <a asp-controller="Warehouse" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-primary mx-2">
                                Edit
                            </a>
                            <a asp-controller="Warehouse" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2">
                                Delete
                            </a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>